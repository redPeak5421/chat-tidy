# Chat Tidy

[English](README.md) | 简体中文

一款浏览器扩展，支持在侧边栏勾选并批量管理 ChatGPT、Claude、Grok、Gemini、Kimi 和 Qwen 的聊天记录。

## 安装

在 Chrome 或其他 Chromium 浏览器的扩展管理页面开启“开发者模式”，选择“加载已解压的扩展程序”，加载本目录。安装或更新后，请刷新网站页面。

## 使用

勾选聊天记录，打开“聊天”标题旁的管理菜单，即可取消选择、反选或全选当前已加载的记录。删除前核对列表，统一确认一次即可。向下滚动侧边栏可以加载更多记录。Chat Tidy 使用各网站原生的删除能力，扩展本身不提供撤销功能；Grok 使用原生软删除操作。

扩展弹窗提供启用开关、复选框显示方式、删除并发数和六种界面语言：简体中文、繁體中文、English、Français、日本語、Русский。选择隐藏复选框时，鼠标靠近聊天左侧即可展开；已勾选或获得键盘焦点的记录仍会显示复选框。

ChatGPT 和 Grok 可设置 1–3 个并发删除请求。Claude 从发起操作的页面发送同源请求，调用网站原生批量删除接口，每组最多 20 条，组间间隔 500 毫秒；并发设置适用于 ChatGPT 和 Grok。遇到 403 会立即停止，不重试或切换接口；遇到限流会停止批次、进入冷却并降低并发数。点击停止可阻止尚未发出的请求，已经发出的请求无法撤回。请保持发起操作的页面打开，直到处理结束。

ChatGPT 支持批量归档。通过侧边栏管理菜单或扩展弹窗的“归档管理”，可多选、批量恢复或删除归档；网站原生归档界面也会增加批量工具。全选仅作用于已加载项，“加载更多”继续读取列表。失败项保留选择，删除前需确认。Grok 尚未确认有原生聊天归档能力，因此不提供归档，也不使用本地隐藏来替代。

Kimi 和 Qwen 支持在侧边栏批量删除。Kimi 的记录行是原生链接，删除通过网站自身的 Connect RPC 聊天服务逐条发送，使用页面已有的登录令牌。Qwen 的记录行不带标识，Chat Tidy 会读取网站自己的聊天列表，按标题和顺序匹配侧栏行；无法匹配的行不显示复选框。Qwen 还支持批量归档和“归档管理”，可恢复或删除已归档的对话。

ChatGPT 和 Qwen 的聊天可通过管理菜单批量移动至项目，Claude 的聊天可移动至组：选择已有目标，或输入名称先新建再移动；新建失败时不会移动任何聊天。ChatGPT 逐条更新会话所属项目，Claude 逐条更新会话记录所属的组，Qwen 每次请求最多加入 20 条。移动后的 ChatGPT 和 Qwen 聊天会离开主列表，Claude 的聊天仍保留在最近列表中。Cowork 任务不支持移动。普通 Claude 聊天不再显示“归档”菜单项，仅当所选全部为 Cowork 任务时才出现。

弹窗高度随内容变化：折叠使用说明后不再留空，展开后默认语言下可完整显示。设置页列出了各网站支持的能力。

Cowork 使用原生会话接口，每次请求处理一个任务，不受并发设置影响。部分任务需要设备证明，扩展不会生成或复制此类证明；请求被拒绝后会停止，需使用 Claude 原生界面处理。只有收到成功响应后，记录才会从列表中消失。

Grok 的“查看全部”搜索窗口也支持复选框和顶部批量删除工具栏。在 Grok 页面打开扩展弹窗，可使用“展示全部”开关，通过原生分页接口补充侧栏聊天记录；关闭后恢复原始列表。历史记录信息仅保存在当前页面内存中，刷新后丢弃；加载请求失败时会停止。

弹窗底部“设置”可选择跟随系统（默认）、白天或黑夜，保存于浏览器本地。Grok 页面还提供“隐藏 Bots”和“默认折叠 Bots”，默认均关闭，隐藏优先；折叠只是刷新后的默认状态，之后仍可手动展开。Chrome 版本另有“隐藏支持菜单”，可连同折叠标题一起隐藏。

## 隐私与兼容性

仅在 chatgpt.com、claude.ai、grok.com、gemini.google.com、www.kimi.com 和 chat.qwen.ai 运行。偏好设置和冷却状态保存在本地。聊天标题、标识符和会话凭据仅在执行用户请求的操作时临时使用。不使用开发者服务器，不含广告或分析追踪。网站接口变化可能影响兼容性。

Claude 的选择范围限定在当前工作区。Cowork 任务支持选择、删除和原生归档；普通 Claude 聊天不支持归档，仅选择 Cowork 任务时才可启用归档。Grok 机器人不在处理范围内。扩展调用的是网站自身接口，并非有公开文档的开发者 API；接口行为已对照网站代码核查，自动化测试使用模拟响应。ChatGPT 与 Claude 的新建项目 / 组接口以及 Kimi 可选的设备风控请求头仅对照公开客户端代码核对，未在真实账号上执行。

## 开发

需要 Node.js 和 npm。

```sh
npm ci
npm run build:i18n
npm run check
npm test
```

测试使用虚构聊天记录和模拟响应，不会删除真实聊天。

翻译维护在 `_locales/*/messages.json` 中。修改后运行 `npm run build:i18n`。生成的语言包支持手动切换语言，Chrome 使用相同的翻译文件展示扩展清单文本。中文提供简体和繁体，不单独区分地区版本。

发布时打包 `manifest.json`、`popup.html`、`src/`、`_locales/`、`icons/` 和 `LICENSE`。

## 支持项目

如果 Chat Tidy 对你有帮助，欢迎通过爱发电或 Ko-fi 支持项目开发。

<p>
  <a href="https://ko-fi.com/U2C326XLKJ"><img src="https://ko-fi.com/img/githubbutton_sm.svg" alt="通过 Ko-fi 支持项目" width="223" height="30" /></a>&nbsp;&nbsp;
  <a href="https://afdian.com/a/redPeak5421"><img src="docs/assets/afdian-zh-CN.svg" alt="通过爱发电支持项目" width="223" height="30" /></a>
</p>

## 许可证

本项目采用 [Apache License 2.0](LICENSE)。GitHub 标志为官方品牌素材，用于链接本仓库，遵循 [GitHub 品牌规范](https://brand.github.com/foundations/logo)，不属于本项目 Apache 许可证的授权范围。

Gemini：管理图标位于“最近”标题同一行。勾选会话后可批量删除，直接使用网站同源接口逐条执行；支持停止并保留失败项选择。更新扩展后请刷新网页。
